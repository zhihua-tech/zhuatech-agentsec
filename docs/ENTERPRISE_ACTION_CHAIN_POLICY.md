# Agent 工具链安全门禁

`POST /api/agentsec/insights/action-chain-policy` 在多步 Agent 计划运行前逐步检查权限范围、参数结构、不可信输入、秘密访问、外部网络目标、外部写入、不可逆操作和人工确认。

系统识别“读取秘密后外传”“不可信内容驱动写入”“未批准权限升级”等组合风险，输出 `ALLOW`、`REVIEW` 或 `DENY`、0–100 风险分、高风险步骤、阻断原因、控制措施和稳定执行轨迹摘要。

接口只做防御性策略判断，不包含攻击载荷，也不会真实执行任何工具；生产部署时应在工具代理层强制执行门禁结论。
